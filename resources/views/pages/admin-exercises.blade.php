@extends('layouts.admin', ['title' => 'Lexi | Admin Exercises', 'description' => 'Visor real de ejercicios e intentos en Lexi Admin.', 'sidebarNoteTitle' => 'Exercises', 'sidebarNoteText' => 'Inventario real de ejercicios y actividad registrada.'])

@section('content')
<section class="admin-page-head"><div><h1>Exercises</h1><p>Vista real de ejercicios creados e intentos registrados.</p></div><div class="admin-page-actions"><a class="admin-btn admin-btn--ghost" href="admin.html"><i class="bi bi-arrow-left"></i> Panel</a></div></section><section class="admin-stats"><article class="admin-card admin-stat"><div class="admin-stat__row"><span class="admin-chip admin-chip--violet">Publicados</span><div class="admin-stat__icon"><i class="bi bi-ui-checks-grid"></i></div></div><p class="admin-stat__value">{{ number_format($stats['published']) }}</p><p class="admin-stat__label">Ejercicios registrados</p><span class="admin-stat__meta"><i class="bi bi-play-circle"></i> Tabla real de exercises</span></article><article class="admin-card admin-stat"><div class="admin-stat__row"><span class="admin-chip admin-chip--amber">Intentos</span><div class="admin-stat__icon"><i class="bi bi-pencil-square"></i></div></div><p class="admin-stat__value">{{ number_format($stats['attempts']) }}</p><p class="admin-stat__label">Intentos registrados</p><span class="admin-stat__meta"><i class="bi bi-hourglass-split"></i> {{ number_format($stats['drafts']) }} borradores</span></article></section><section class="admin-card"><div class="admin-card__inner"><div class="admin-card__head"><div><h2>Exercise catalog</h2><p>Listado real de ejercicios disponibles para administración.</p></div></div><form class="row g-3 mb-4" method="get" action="{{ route('admin-exercises') }}"><div class="col-md-10"><label class="form-label" for="exerciseSearch">Buscar ejercicio</label><input class="form-control" id="exerciseSearch" type="search" name="q" value="{{ $filters['q'] }}" placeholder="reading, listening, writing..."></div><div class="col-md-2 d-flex align-items-end"><button class="admin-btn admin-btn--primary w-100" type="submit"><i class="bi bi-search"></i> Filtrar</button></div></form><div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>ID</th><th>Title</th><th>Type</th><th>Source</th><th>Status</th><th>Attempts</th></tr></thead><tbody>@forelse($exercises as $exercise)<tr><td>{{ $exercise->id }}</td><td>{{ $exercise->title ?: 'Sin título' }}</td><td>{{ ucfirst($exercise->type) }}</td><td>{{ $exercise->source ?: 'manual' }}</td><td><span class="admin-status {{ $exercise->attempts_total > 0 ? 'admin-status--active' : 'admin-status--review' }}">{{ $exercise->attempts_total > 0 ? 'Con uso' : 'Sin intentos' }}</span></td><td>{{ number_format($exercise->attempts_total) }}</td></tr>@empty<tr><td colspan="6">No hay ejercicios para ese filtro.</td></tr>@endforelse</tbody></table></div>@if ($exercises->hasPages())<div class="d-flex justify-content-between align-items-center pt-3"><p class="mb-0 text-muted small">Mostrando {{ $exercises->firstItem() }}-{{ $exercises->lastItem() }} de {{ $exercises->total() }} filas.</p><div>{{ $exercises->onEachSide(1)->links() }}</div></div>@endif</div></section>
@endsection